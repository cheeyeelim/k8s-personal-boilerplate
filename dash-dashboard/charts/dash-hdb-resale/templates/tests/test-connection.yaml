apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dash-hdb-resale.fullname" . }}-test-connection"
  labels:
    {{- include "dash-hdb-resale.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dash-hdb-resale.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
